# Assets

Static assets for the EnergyPoint website.

- `css/` - future extracted stylesheets
- `images/` - real product, gallery, logo, and showroom images
- `js/` - future extracted scripts

The current site keeps CSS and JavaScript inline in `index.html` and `form.html`
to preserve the zero-build GitHub Pages workflow.
